# ARM Field Notes

This file is the repository-local running log for confirmed field facts, business constraints, deployment lessons, and verified workarounds discovered during ARM cross-compilation, deployment, and runtime verification.

## Update Rules

- Update this file during the work, not only at the end.
- If code behavior, runtime config, or deployment steps change, update the relevant note in the same work cycle.
- Prefer confirmed facts over speculation. Mark assumptions clearly until they are verified.
- When a previous conclusion is superseded, replace or annotate it so the current state is unambiguous.

## Current Confirmed Facts

- As of 2026-04-18, the active reachable compile-host address from this workstation is `jamin@192.168.110.128`. It is the same `jamin` x86_64 compile host; the older `192.168.1.118` path had started timing out during SSH banner exchange in this session.
- The ARM test host is `root@100.105.175.44`.
- Backend builds on the compile host use `onebuild_GOGS_backend_self.sh` from `PATH`; do not hardcode a private script path.
- Before every backend build on the compile host, delete the existing backend executable output first so a stale `GrabSystem` cannot be mistaken for a new artifact.
- Backend replacement on the test host must stop `gogs-backend.service` before copying `/userdata/GOGS/backend/GrabSystem`.
- The reliable backend replacement flow on the test host is:
  1. stop `gogs-backend.service`
  2. upload the new binary to `/tmp/GrabSystem.arm.new`
  3. back up the current binary
  4. clear `/userdata/GOGS/backend/logs/*`
  5. copy the new binary into `/userdata/GOGS/backend/GrabSystem`
  6. `chmod +x` the binary
  7. start `gogs-backend.service`
  8. verify service state and runtime behavior
- The deployed target is an RK3588-class device with limited headroom. Point-cloud and video changes must optimize for end-to-end throughput, low CPU overhead, low memory bandwidth pressure, and low transfer cost.
- Radar `51` and `52` are two independent devices, not one split data source.
- `192.168.0.51` 雷达与 `192.168.0.152` 摄像头是当前现场确认可联通的一组设备；当现场切换到这组设备时，后端运行配置也必须同步切到 `lidar/ip=192.168.0.51` 和对应的 `camera/*` 地址。旧的 `192.168.0.52` 雷达与 `192.168.0.133` 摄像头当前已关闭，不应再作为默认联调组合。
- For code synchronization, the default published-code path is still to push and let the compile host `git pull`. But if the task is explicitly to deploy the current dirty local workspace, `scripts/arm/build_backend_remote.sh` is not enough by itself; first `rsync` the local tree to `/home/jamin/qt/GrabInventorySystem` on the compile host, then run `onebuild_GOGS_backend_self.sh` there so uncommitted local changes actually enter the ARM build.
- For single-frame point-cloud display, `processing/lidar_max_visible_radius = 0` must not fall back to the ROI half-diagonal. When the radius is not configured, estimate a generous adaptive outer radius from the current frame's radial distribution so the main cloud is preserved while isolated far-edge noise can still be excluded.
- The monitor page frontend does not crop point clouds to one quadrant. It renders backend-provided `x/y/z` coordinates directly into Three.js, so "only one quadrant is visible" is more likely to be a backend filtering effect or a camera framing issue than a frontend axis clip.
- For the monitor page point-cloud view, single-frame display should not render the highest-point marker. Keep highest-point data for other panels, but avoid overlaying that marker on the live single-frame cloud.
- On the current field deployment, the mediamtx embedded HLS player page can return HTML while its relative static assets still resolve to `404`, so once the camera stream is switched to `H264`, prefer direct HLS playback in the frontend instead of embedding `/media/hls/<path>/`.
- For the monitor page single-frame point-cloud display, the field requirement is now stricter still: show the raw lidar frame directly with full point count first. The display path must not apply coordinate transform, translation, rotation, PLC pose, calibration extrinsics, display-side voxel downsampling, or display-side point-count limiting. Algorithm and measurement filters remain separate.
- For monitor-page single-frame point-cloud transport, align with the official `LidarView` callback chain: push the SDK raw frame directly to the frontend WebSocket path instead of routing single-frame display through `PointCloudProcessor`.
- ARM 发布链路现在应优先使用 `scripts/arm/` 下的阶段化脚本，而不是在会话里反复拼接长串 `ssh` / `rsync` one-liner。目标是把输出收敛成 `PASS/FAIL + 摘要 + 日志文件`。
- 固定阶段应收敛为：`preflight`、`build_backend`、`deploy_backend`、`deploy_frontend`、`verify`。每个阶段必须有强判定条件，原始日志默认下沉到 `logs/arm/`，只有失败时才回显尾部关键信息。
- `scripts/arm/pipeline.sh` 默认只负责“已存在代码”的构建、部署、验活，不自动执行 `git add/commit/push`。代码发布边界必须和代码变更边界分开。
- 前端部署阶段应支持“跳过本地重建，只同步现有 `frontend/dist`”的模式；这适合只验证前端同步/重载链路，不应每次都重复耗时构建。
- `verify_remote.sh` 不应依赖需要登录态的 HTTP API。视频专项验活优先看首页资源和 HLS `m3u8`，PTZ 专项验活优先看 `VideoManager: ONVIF connection state changed to connected` 这样的后端日志证据。
- 当前 ARM 交叉编译链在链接 Tanway 官方 ILidarAlgo 时，不能只靠 CMAKE_SYSTEM_PROCESSOR 命中 aarch64 分支；需要同时兼容 CMAKE_CROSSCOMPILING，否则会出现 `undefined reference to tanway::ILidarAlgo::Create`。
- 官方 Tanway `algo_table.json` 不能只停留在仓库源码里。ARM 发布时必须同步到测试机 `/userdata/GOGS/backend/config/`，并保证运行时路径解析优先覆盖 `backend/config/algo_table.json` 这类现场布局。
- 2026-04-16 测试机现场确认：当前雷达设备 `192.168.0.52` 走测试机 `eth1`，主机接收地址为 `192.168.0.17`；`ip route get 192.168.0.52` 的实际结果是 `dev eth1 src 192.168.0.17`，且 `ping 192.168.0.52` 可达。
- 如果运行配置中的 `tanway_sdk/lidar_config/on-line_config/host_ip` 指向一个本机不存在的地址，例如过期的 `192.168.111.204`，Tanway SDK 在线模式会持续报 `Create or bind socket` 并最终 `Lidar disconnected`。当前后端已修复为：当配置的 `host_ip` 不是本机有效 IPv4 时，自动按雷达源地址探测同网段本机 IP 后继续启动。
- 2026-04-16 的现场验证结果表明，Tanway `warning (97): Enable time window mode failed!` 在当前设备上是非致命告警；只要后端持续出现 `Tanway SDK point cloud callback` 和 `Point cloud diagnostics`，点云主链路就是正常的，不应把这条告警误判成“点云未显示”的根因。
- 2026-04-16 09:22-09:25 的最新一次完整 ARM 发布已验证通过：编译机产物 `GrabSystem` 的 `sha256` 为 `88453e06c92bf03f0bf41079deb6db6a5546ac12bbf650922f3c212ac9fd79cb`，测试机运行中二进制哈希一致；前端首页最新资源为 `assets/index-CChVQXNS.js`；测试机本地 `curl http://127.0.0.1/media/hls/camera/index.m3u8` 返回 `#EXTM3U`；`journalctl -u gogs-backend.service` 同时出现了 `VideoManager: ONVIF connection state changed to connected` 和 `Tanway SDK point cloud callback #443 points=14913`，说明本次部署后的后端服务、视频代理、PTZ 接通状态和雷达点云回调都处于可验活状态。
- 2026-04-16 09:51-10:03 的点云角度裁切联调已经确认：对当前现场 `LT_FocusB2_64`，把 SDK FOV 直接放到 `0.01~360.00` 会让新进程只收到 DIF/标定帧而不再触发 `OnPointCloud()`；原因是 Tanway 非 TW360 机型在非 time-window 模式下仍依赖 `point.angle < m_startAngle` 的过零条件做组帧，起始角过低会抑制回调。这个结论只证明 `0.01~360.00` 过宽会压掉回调，不再适合作为“关闭裁切”的默认值。
- 2026-04-16 10:20 之后的现场要求已收紧：当前阶段必须先解决“前端显示点数是否与后端雷达原始接收点数一致”，再讨论点云是否完整。为此，单帧 WebSocket 显示链已改成优先全量发送有限点，不再读取 `processing/voxel_size` 做显示压缩。
- 2026-04-16 10:22 的后端部署产物 `GrabSystem` `sha256` 为 `b2e6d1dfef276d793069e1a506376c26f1440c0e86c67c21d40ca284bdcf2307`，测试机 `deploy_backend.sh` 已验证运行中二进制哈希一致且 `gogs-backend.service` 为 `active`。
- 2026-04-16 10:22 的测试机运行配置已确认：`processing/display_frame_directional_clip_enabled=false`，因此当前单帧显示链没有额外方向裁切；SDK 角度范围仍显式设置为 `launch\\fov_enabled=true`、`launch\\fov_min_deg=0.1`、`launch\\fov_max_deg=179.9`，也就是当前现场依旧只放开安全半角，不是 360 全角。
- 2026-04-16 10:23-10:27 的运行日志表明：后端持续出现 `Tanway SDK point cloud callback`，说明雷达原始回调稳定；同时日志中也能看到 `Authenticated WebSocket client connected` 后紧接 `Client disconnected`。因此“雷达没出点”和“客户端稳定拿到单帧点云”必须分开判断，后续核对 `rawCount/displayCount/points.length` 时要优先用直连 `12345` 或测试机本机回环路径，避免把代理链路抖动误判成点云链路本身的问题。
- 2026-04-16 11:03 的最新后端修复已确认根因：`tanway_sdk/launch/fov_enabled=false` 在代码里并不是真正的“关闭 SDK 角度裁切”，而是仍然对 `LT_FocusB2_64` 下发了默认 `0.10~179.90`，这正是单帧点云长期只显示半圈的直接原因。
- 2026-04-16 11:03 的已部署后端产物 `GrabSystem` `sha256` 为 `09a5555d481c71fd79df1e3cf1b3841492ef55d14c927b9c64cd981cf2ece87f`。修复后测试机启动日志明确变成 `Tanway SDK angle range: enabled=false min=0.10 max=359.90 defaultMin=0.10 defaultMax=359.90`，随后 `Tanway SDK point cloud callback` 的角度范围稳定达到 `minAngle≈0.11 maxAngle≈359.87 span≈359.76`，说明在当前 `LT_FocusB2_64` 现场设备上，安全近全角应以 `0.10~359.90` 为默认策略，而不是 `0.10~179.90`。
- 2026-04-16 11:03 的同一轮运行日志还确认：单帧 WebSocket 广播链继续保持 `raw=finite=display`，例如 `raw=182754 finite=182754 display=182754`、`raw=129078 finite=129078 display=129078`。因此当前后端单帧显示链已经同时满足“SDK 回调近全角”和“后端发送点数不缩水”这两个条件，后续如果前端还显示不完整，优先怀疑页面未刷新、旧 bundle/旧 WebSocket 会话残留，或观察视角问题，而不是后端单帧传输链继续丢点。
- 2026-04-16 11:48 的后端部署产物 `GrabSystem` `sha256` 为 `00734d792b619ef3962db9f1d20424c5c1c1491b6c07b7d9175e7b452c5df17b`。这轮修复把单帧全量点云的超大 JSON 打包频率降到低频，并清理了死 WebSocket 客户端；测试机随后确认 `GET /api/status` 立即返回 `200`、未鉴权 `GET /api/system/info` 立即返回 `401`、`POST /api/auth/login` 立即返回 `200`，说明“前端显示未连接后端”的直接根因已经从运行态上解除。
- 2026-04-16 11:48 的同一轮测试机连接状态也确认：`ss -tan` 下 `:8080` 和 `:12345` 已不再持续堆积 `CLOSE-WAIT`，只剩 `LISTEN/ESTAB/TIME-WAIT` 为主。此前那种 API 长时间无响应且半关闭连接堆积的状态，和“全量单帧点云高频打包 + 无效连接未及时回收”直接相关。
- 2026-04-16 11:48 之后的后端日志还确认：SDK `ERR_TIME_WINDOW` 告警已改成 5 秒节流汇总，例如 `Tanway SDK non-fatal time-window warning (97): ... (suppressed=48)`；单帧广播则保持 `raw=finite=display` 但降为低频完整帧，例如 `raw=593061 finite=593061 display=593061`。这说明当前策略是“保全量点数，牺牲刷新频率”，而不是重新引入降采样或显示侧裁剪。
- 2026-04-16 11:49 的前端部署已同步到测试机，首页最新资源为 `assets/index-B2x9_Zpc.js`。本地 `frontend/tests/*.test.mjs` 这轮为 `112/112` 通过，包含 `RemoteOperationView` 的布局约束修复。
- 2026-04-16 13:04 的前端再部署后，测试机首页最新资源变为 `assets/index-Cgzf12Iy.js`，对应的新监控页 bundle 为 `assets/MonitorView-e_16y3do.js`，并且 `dist/assets/system-OgvjECzK.js` 已包含 `pointCloudMeta` / `pointCloudChunk` 的组包逻辑。
- 2026-04-16 13:10 的最新后端部署产物 `GrabSystem` `sha256` 为 `6732f10d03aa4e2bee22fc5b0346e13c9342669a56428fbf3707260cb0209a4d`。这轮把单帧点云分块大小从 `20000` 进一步收紧到 `2000` 点每块，以降低单条 WebSocket 文本消息体积。
- 2026-04-16 13:11-13:12 的 nginx 访问日志已确认：监控页 `/ws` 握手返回 `101`，而且单个连接实际传输了数 MB 到数十 MB 数据，例如 `33460090`、`24944979`、`21543116` 字节。这说明分块点云数据已经经由 nginx `/ws` 实际送往浏览器，当前“点云不显示”的主要矛盾已不在后端是否发出了数据。
- 2026-04-16 13:12 的同一批现场日志还确认：测试机浏览器最近没有重新请求 `assets/index-Cgzf12Iy.js` 或 `assets/MonitorView-e_16y3do.js`，访问日志里仍只看到更早的旧资源 `assets/MonitorView-CVm2ZA_k.js`。因此如果页面没有做强制刷新，就会继续运行旧 bundle；旧 bundle 不认识新的 `pointCloudMeta` / `pointCloudChunk` 协议，即使 WebSocket 已经收到了大量点云字节，也会表现成“视频正常、点云不显示”。
- 2026-04-16 13:11:51 曾出现一次 `gogs-backend.service` `SIGSEGV`，发生在多客户端 WebSocket 传输高峰之后；systemd 随后已自动重启服务，当前运行中的主进程已恢复并继续稳定广播 `mode=chunked` 的单帧点云。这一崩溃属于已确认风险，后续仍需继续盯防，但它不能解释“当前页面完全不显示点云”的全部现象，因为 `/ws` 101 和多 MB 传输证据表明数据随后仍持续送达浏览器。
- 2026-04-16 当前现场最终确认结果：监控页单帧点云已经恢复为完整显示。形成这个结果的必要条件是三项同时成立：后端 SDK FOV 默认改为安全近全角 `0.10~359.90`、单帧链路改为 `raw=finite=display` 的分块 WebSocket 传输、以及浏览器实际加载到新的前端 bundle。后续如果再出现“视频正常但点云不显示”，优先检查浏览器是否仍在执行旧 bundle，而不是先怀疑雷达没出点。
- 2026-04-16 本轮代码结果已经提交并推送到 `origin/master`，提交为 `a7ea44a8a76bad539847a524ca0d8fbe15ed2bca` (`fix monitor point cloud delivery and frontend runtime polish`)。后续若需回溯“点云完整显示”的代码基线，应从这个提交开始对比。
- 2026-04-16 基于当前已确认正确的完整单帧点云，可以把 Tanway 原始点云坐标语义收敛为：`angle` 是 XOY 平面内相对 `+X` 的水平角，范围定义为 `0°~359.99°`；按当前系统的原始单帧显示链，前端直接渲染 SDK 给出的 `x/y/z`，不再经过 PLC/world 坐标变换，所以 `0°≈+X`、`90°≈+Y`、`180°≈-X`、`270°≈-Y` 的判断对“当前页面看到的单帧点云”成立。
- 2026-04-16 当前现场“有效角度”的运行态事实应以最新日志为准，而不是再沿用 SDK 文档里“通常 30°~150°”的默认/常见描述。测试机当前完整单帧链路下，SDK 启动日志为 `enabled=false min=0.10 max=359.90`，回调日志长期稳定在 `minAngle≈0.11 maxAngle≈359.87 span≈359.7`，因此当前现场单帧显示应视为“近全 360° 有效水平角”，不是 `30°~150°` 扇区。
- 2026-04-16 后续性能策略已调整为：单帧显示链不再发送“完全原始全量点”，而是保持“只做现有方向裁切 + 一次 `processing/voxel_size` 体素降采样”。不恢复目标点数估算、不恢复显示侧限点、不叠加其他单帧处理步骤；这样既保留链路语义简单，也能直接降低 WebSocket 分块数量和前端渲染压力。
- 2026-04-16 13:55-13:57 的最新联合发布已完成：编译机后端产物 `GrabSystem` `sha256` 为 `e3f3c78f6974eaac948571add94e7df1f640c888b75a4c8b027fc51345ac7537`，测试机运行中二进制哈希一致；前端首页资源已切到 `assets/index-uBPaGNpx.js`。
- 2026-04-16 13:57 的测试机运行日志已确认本轮单帧性能调整生效：点云回调仍保持近全角，例如 `points=629223 minAngle=0.11 maxAngle=359.85 span=359.74`，而单帧广播改为带降采样输出，例如 `raw=629223 finite=629223 display=8814 mode=chunked`、`raw=354962 ... display=8269 mode=chunked`。这说明当前测试机单帧显示链已经切换成“原始回调近全角 + 显示前体素降采样 + 分块发送”的运行态。
- 2026-04-16 14:14-14:15 的监控页 PTZ 回归已确认根因在前端而不是后端：测试机 `GET /api/video/ptz/status` 正常，但浏览器没有发出任何 `POST /api/video/ptz/move|stop|relative-move|home|presets`。`frontend/src/views/MonitorView.vue` 当时把 `ptzStart/ptzStop/onZoomChange/savePreset` 留成了 `console.log`/本地赋值桩，导致“视频正常但云台控制和缩放完全没生效”。
- 2026-04-16 14:14-14:15 的修复已恢复监控页 PTZ 全链路：`ptzStart -> moveVideoPtz`、`ptzStop -> stopVideoPtz(true, false)`、`resetPtz -> gotoVideoPtzHome()`、`onZoomChange -> relativeMoveVideoPtz(0, 0, delta)`、`savePreset -> saveVideoPtzPreset(...)`，并保留“预置位仅在打开时懒加载”的策略。
- 2026-04-16 14:14 的前端契约测试已全绿：`cd frontend && node --test tests/*.test.mjs` 结果为 `120/120` 通过，其中新增了 `monitor view wires PTZ movement, stop, reset, zoom, and preset actions to backend video APIs` 回归用例，专门防止 `MonitorView` 再次把 PTZ 事件断成空实现。
- 2026-04-16 14:14 的前端本地构建已通过，`vite build` 产出的测试机首页最新资源为 `assets/index-B4DMz39S.js`。后续如果现场再出现“后端 PTZ 状态 ready，但按钮没反应”，先检查浏览器是否已经刷新到这个或更新的 bundle，再查后端 ONVIF/PTZ 链路。
- 2026-04-16 14:20-14:23 的“全暗色空白页”现象，现有证据更像默认首页 `/dashboard` 的重型图表 bundle 阻塞了路由首屏，而不是 nginx/后端整体不可达：当时 `GET /`、`GET /assets/index-*.js`、`GET /ws`、`POST /api/auth/refresh` 都能在部分客户端恢复到 `200/101`，但对 `100.114.245.115` 这一路浏览器，请求日志只看到 `LayoutView`、`DashboardView`、`vendor-chart` 等静态 chunk，被动等待阶段没有继续发出任何 `Dashboard` 数据 API 请求，说明页面很可能卡在首页路由组件解析而不是接口业务逻辑本身。
- 2026-04-16 14:28 的前端修复已把 `frontend/src/views/DashboardView.vue` 从“顶层静态引入 `../utils/echarts.js`”改成“组件挂载后 `ensureECharts()` 懒加载”。这样 `/dashboard` 会先渲染页面壳和基础静态内容，再异步加载图表库，避免 `vendor-chart` 在首页首帧阶段阻塞整页。
- 2026-04-16 14:28 的回归约束已补到 `frontend/tests/interface-alignment-contract.test.mjs`：`dashboard defers heavy echarts loading until after the route shell mounts`。本地 `cd frontend && node --test tests/*.test.mjs` 结果已更新为 `121/121` 通过。
- 2026-04-16 14:29 已将上述首页首帧修复同步到测试机前端，部署脚本回执为 `[PASS] deploy_frontend: skip_build=1 index=assets/index-YE2ZyFIk.js`，测试机当前首页资源已切到 `assets/index-YE2ZyFIk.js`。后续如果用户反馈“打开首页只有深色背景、没有内容”，先确认浏览器是否已强制刷新到这个或更新的 bundle。
- 2026-04-16 15:06 的现场稳定性问题已确认：仅关闭 `processing/enable_map_building` 不能阻止后端 OOM，`GrabSystem` 仍会在约 97 秒内从 `1.2G RSS` 增长到 `5.5G RSS`。真正需要切断的是 `PointCloudProcessor` 整条后台处理链，而不只是 `GlobalMapManager` 的实时地图快照。
- 2026-04-16 15:06 新增了运行时开关 `processing/enable_point_cloud_processing`。语义是“关闭 PointCloudProcessor 后台处理链，但保留原始单帧 WebSocket 点云显示”；后端配置中心 schema、`ConfigManager`、`Application`、`HttpServer`、`PointCloudProcessor` 已全部接通，本地 `frontend/tests/*.test.mjs` 为 `126/126` 通过。
- 2026-04-16 15:06 测试机 `/userdata/GOGS/backend/config/config.ini` 的真正生效格式是 `[processing]` 段内的 `enable_point_cloud_processing=` / `enable_map_building=`，不是文件尾部追加的 `processing\\...=` 根键。现场一度因为误把 false 追加成根键，导致后端继续按 `[processing]` 段里原来的 `true` 运行。
- 2026-04-16 15:06 将测试机 `[processing]` 段中的 `enable_point_cloud_processing=false`、`enable_map_building=false` 后，最新后端进程 `4161180` 在 `15:06:41` 之后的日志已不再出现 `Point cloud diagnostics`，只剩 `Tanway SDK point cloud callback`、`Frame point-cloud broadcast` 和背压日志，说明后台处理链已被真正切断，而原始单帧显示链仍在工作。
- 2026-04-16 15:06 同一轮现场内存观测结果也已确认稳定：最新进程 `4161180` 的 RSS 在约 `85` 秒内仅从 `540MB` 增长到 `598MB`，不再出现此前每分钟数 GB 的失控上涨。对于“前端一直连不上/连上后很快失联”的这类现场症状，当前最稳的应急基线就是保持 `[processing] enable_point_cloud_processing=false`。
- 2026-04-16 16:20 的当前工作区已引入新的 `FusionSegmentService` 段式融合骨架：`Application.cpp` 里 `LidarDriver::pointCloudReceived` 的默认主接线已经从 `PointCloudProcessor::processFrame` 切到 `FusionSegmentService::processFrame`；`fusion/*` 与 `monitor/auto_switch_to_fusion` 配置键已经接入 `SystemConfig`、`ConfigManager` 和 `HttpServer` schema。
- 2026-04-16 16:20 的当前代码里，`FusionSegmentService` 直接维护段级 canonical surface cells，并在 `stationary / distance_or_time / trajectory_segment / flush` 时提交 `fusion preview` 与 `measurement cloud`。`PointCloudProcessor` 线程仍保留并启动，但不再作为默认融合主路径。
- 2026-04-16 16:20 已确认一个迁移期关键点：新的 `fusion preview` 不能再受旧 `processing/enable_map_building` 语义直接约束，否则测试机上沿用的旧稳定性配置会把新融合预览一起关掉。当前代码已改成 `FusionSegmentService` 的 preview 跟随 `fusion/enabled`，而不是旧 `enable_map_building`。
- 2026-04-16 16:20 本地工作站无法直接配置 backend CMake，因为缺少 `Qt6Config.cmake`；本轮后端编译验证已改走编译机 `jamin@192.168.1.118`。
- 2026-04-16 16:20 编译机上对当前脏工作区执行 `rsync -> onebuild_GOGS_backend_self.sh` 已通过，ARM backend 产物 `GrabSystem` `sha256` 为 `35973489f23c47f7688f13f07031d3ac1bf06f390bca129f5d01450da74d1572`。
- 2026-04-16 16:20 编译机上直接跑 `ctest -R GrabSystemRegressionTests` 会失败，不是测试逻辑本身错误，而是因为 `GrabSystemRegressionTests` 也是交叉编译出来的 ARM 二进制，x86 编译机无法直接执行，报 `Syntax error: word unexpected`。后续若要验证这些 regression tests，需要在 ARM 目标机或可运行 ARM 二进制的环境执行。
- 2026-04-16 17:07 已确认新的 backend regression 验证路径应该固定为“编译机生成 ARM `GrabSystemRegressionTests` -> 同步到测试机 `/tmp/GrabSystemRegressionTests.arm` -> 在测试机设置 `GOGS_REPO_ROOT=/tmp/GOGS-backend-source` 后执行”。当前跑通的测试二进制 `sha256` 为 `75f4ffe4ad876c57af1bfdb86c1e46aa74e0f2388e732d1480f224b35f633080`，对应运行日志是 `logs/arm/20260416-170735-testhost-regression.log`，结尾明确为 `All regression checks passed`。
- 2026-04-16 17:07 这轮 regression 套件已按当前架构收敛：`PointCloudProcessor` 的若干用例改为走同步 `processFrameInternal()`，避免测试再依赖异步线程调度；测试机源码契约检查通过环境变量 `GOGS_REPO_ROOT=/tmp/GOGS-backend-source` 定位 `Application.cpp` / `ConfigManager.cpp` / `HttpServer.cpp`；盲区低支持度用例当前稳定校验的是“低支持度诊断已暴露”，而不是再强绑“必须升级成 rescan 建议”。
- 2026-04-16 17:09 的当前整体验活已确认：测试机运行中后端 `GrabSystem` `sha256=35973489f23c47f7688f13f07031d3ac1bf06f390bca129f5d01450da74d1572`，`scripts/arm/verify_remote.sh` 返回 `PASS`；前端重新部署后首页资源为 `assets/index-BfO9w8JO.js`；本地前端契约测试 `cd frontend && node --test tests/*.test.mjs` 为 `132/132` 通过。
- 2026-04-16 17:09 曾经暴露过一个 `verify_remote.sh` 摘要解析风险：早期版本如果按固定输出位置取 `ptz_line / radar_line`，在 `journalctl` 某一项未命中时可能出现摘要字段错位。当前脚本已改为带 `service=/sha=/index=/hls=/ptz=/radar=` 前缀的键值输出，并由 `frontend/tests/arm-verify-remote-contract.test.mjs` 约束“不允许退回固定行号解析”；现场判断仍建议优先结合原始 verify log 和单独 `journalctl`，但不再需要沿用旧的“字段名可能错位”判断。
- 2026-04-16 17:29-17:40 的“首次刷新进入监控页后只剩深色空白界面”问题，现有现场证据更像前端运行时在 `MonitorView` 点云视口初始化阶段抛错，而不是后端接口或静态资源不可达：当时 `GET /monitor`、`GET /assets/index-BfO9w8JO.js`、`GET /assets/MonitorView-*.js`、`POST /api/auth/refresh` 和 `GET /ws` 都能成功，但同一路浏览器没有继续发出 `GET /api/video/stream-info`、`/api/video/recording/status`、`/api/video/self-check`、`/api/video/ptz/status`、`/api/config`、`/api/piles`。这与 `MonitorView` 的 `onMounted()` 先 `await ensurePointCloudViewport()`、一旦 WebGL 初始化失败就整段跳过后续加载完全吻合。
- 2026-04-16 17:40 的修复已将 `frontend/src/views/MonitorView.vue` 改为 `safeEnsurePointCloudViewport()` 包装 `ensurePointCloudViewport()`，在点云视口初始化失败时记录 `pointCloudViewportError`、释放 Three.js 资源并继续执行后续视频/PTZ/配置加载，不再把整页拖成“深色空壳”。这轮前端契约测试已更新为 `133/133` 通过，测试机前端重新部署后首页资源为 `assets/index-Uiuhbjx-.js`，`scripts/arm/verify_remote.sh` 也已再次返回 `PASS`。
- 2026-04-17 00:31-00:32 的最新一次脏工作区直同步 ARM 发布已再次验证通过：先把当前本地源码 `rsync` 到编译机 `/home/jamin/qt/GrabInventorySystem`，再在编译机删除旧 `GrabSystem` 后执行 `onebuild_GOGS_backend_self.sh`，产物 `sha256=35973489f23c47f7688f13f07031d3ac1bf06f390bca129f5d01450da74d1572`；测试机后端替换后运行中二进制哈希一致；前端重新部署后首页资源为 `assets/index-B-Poip6X.js`；`scripts/arm/verify_remote.sh` 返回 `PASS`，并同时拿到 `#EXTM3U`、`VideoManager: ONVIF connection state changed to connected` 和 `Tanway SDK point cloud callback #103 points=373215`，说明这轮布局修复后的前端产物与当前后端运行态已在 ARM 测试机上完成整体验活。
- 2026-04-18 23:45 之后已确认：`192.168.110.128` 不是新的测试机，而是同一台 `jamin` x86_64 编译机的另一条可达地址；切到这个地址后，`scripts/arm/pipeline.sh --skip-build-frontend` 已重新完成当前工作树的 ARM 构建、后端替换和前端同步。最新后端产物 `GrabSystem` `sha256=3c2283a84efa1de7bda50c5c42e43cbdcb68178981ee45da1946aa59797334e2`，测试机首页资源切到 `assets/index-Bxrnerc2.js`。
- 2026-04-18 23:49-23:50 的 backend regression 再验证已通过：这轮先暴露出 `scripts/arm/run_backend_regression.sh` 只同步 `Application.cpp` / `ConfigManager.cpp` / `HttpServer.cpp` 已不够，因为新的 `fusion preview` 契约回归还会读取 `include/service/WebSocketServer.h` 与 `src/service/WebSocketServer.cpp`。补齐这两个文件后，测试机上的 `rtk bash scripts/arm/run_backend_regression.sh` 返回 `[PASS] backend_regression: sha256=423e79fbe639a1509162b7c6ab6de77ea52033a7fb2022abc81bc0011a5dde1a result='All regression checks passed'`。
- 2026-04-18 23:48-23:49 的 verify 失败原因已确认是现场外设不可达，而不是发布未落地：测试机运行中服务 `gogs-backend.service` 为 `active`，运行中 `GrabSystem` 哈希与本轮部署一致，首页 `GET /` 返回 `200` 且能解析到 `assets/index-Bxrnerc2.js`；但 `ip route get 192.168.0.52` 结果虽仍是 `dev eth1 src 192.168.0.17`，实际 `ping 192.168.0.52` 与 `ping 192.168.0.133` 都是 `100% packet loss`，后端日志持续报 `VideoManager: ONVIF health check failed ... Host unreachable (http://192.168.0.133/onvif/device_service)`，同时 `curl http://127.0.0.1/media/hls/camera/index.m3u8` 返回 `404 page not found`。因此当前剩余边界是测试机到现场雷达/摄像头链路未恢复，不是 ARM 脚本本身仍不可用。
- 2026-04-19 00:36 的最新现场确认已把运行配置切到 `192.168.0.51` 雷达 + `192.168.0.152` 摄像头；测试机 `ping 192.168.0.51` 与 `ping 192.168.0.152` 均为 `0% packet loss`，`curl http://127.0.0.1/media/hls/camera/index.m3u8 | head -n 1` 返回 `#EXTM3U`，后端日志重新出现 `VideoManager: ONVIF connection state changed to connected` 与 `Tanway SDK point cloud callback`，`rtk bash scripts/arm/verify_remote.sh` 也已回到 `PASS`。这条结论同时确认旧组合 `192.168.0.52 / 192.168.0.133` 已不再是当前可用现场设备对。
- 2026-04-19 00:52-00:53 的 ARM 发布已把录像停止收口修复部署到测试机：运行中后端 `GrabSystem` `sha256=8506b211713a4d72d7a9fdc0b827f2514f9d266aa70d05b7f7ad4e52f46ecc7a`，`rtk bash scripts/arm/run_backend_regression.sh` 继续返回 `All regression checks passed`。
- 2026-04-19 00:53 的真实设备复验已确认：分段录像最新样本 `/userdata/GOGS/backend/data/videos/20260419/005943-00000.mp4` 可被 `ffprobe` 正常识别为 `H.264 MP4`，时长约 `9.5s`；此前 `moov atom not found` 的现场问题已解除。
- 2026-04-19 00:59 的第二轮 ARM 发布已把视频文件路径白名单修复部署到测试机：`/api/video-records` 能列出最新分段录像，`/api/video-files/stream` 对该样本返回 `206 Partial Content`，`/api/video-files/download` 返回 `200 OK`。这说明回放文件本体、流式读取和下载三条链路已在目标机运行态闭环。
- 2026-04-19 01:37-01:41 的最新一轮 ARM 收口已再次确认：编译机产物 `GrabSystem` `sha256=5ef7aba908e780d3cac5527dd4445f5cdb81b4a2b0ccf1942356ad918c836104` 已成功部署到测试机，`rtk bash scripts/arm/verify_remote.sh` 返回 `PASS`，并继续命中 `#EXTM3U`、`VideoManager: ONVIF connection state changed to connected` 和 `Tanway SDK point cloud callback`；`rtk bash scripts/arm/verify_video_workflow.sh` 也继续返回 `PASS`，最新样本 `014124-00000.mp4` 通过 `ffprobe`，`stream=206`、`download=200`。
- 2026-04-19 01:30-01:37 的 backend regression 脚本层风险也已收口：旧版 `scripts/arm/run_backend_regression.sh` 因固定使用 `/tmp/GrabSystemRegressionTests.arm`，在前一轮远端进程未退出时会触发 `Text file busy` 并把下一轮卡死。当前脚本已改为每轮独立临时路径 + `timeout` + `trap cleanup EXIT`，这轮重新验证返回 `[PASS] backend_regression: sha256=c1412dd5b9b20013e1ad6ba717bcd8ac2fe5b977afddc1b6b2ede7af93a40fa1 result='All regression checks passed'`。
- 2026-04-19 01:41 的两条新现场专项脚本也已在最新运行态重跑并给出稳定结论：`verify_scale_protocol.sh` 明确返回 `ui/scale_devices is empty`，说明测试机当前还没有称重设备运行态配置；`verify_blind_zone_workflow.sh` 明确返回 `processing diagnostics are not ready`，说明这轮现场还没有形成可用于盲区参数验收的活跃处理诊断数据。这两项仍属于现场输入未就绪，不是仓库内实现缺口。
- 2026-04-19 01:47-01:49 的总验收入口联调又确认了一条脚本契约：`verify_remote.sh` 不能把“最近 5 分钟必须出现 PTZ connected 日志”作为 `core` 阶段硬门槛，因为 `verify_field_acceptance_bundle.sh` 的顺序是先 `core` 再 `video`，近期没有触发 PTZ 动作时会出现假失败。当前脚本已改为：近期 PTZ 日志缺失时只输出 `ptz=not-observed-in-recent-journal-window`，真正的 PTZ 硬验收继续放在 `verify_video_workflow.sh`。
- 2026-04-19 01:49 的最新总验收结果已稳定为：`rtk bash scripts/arm/verify_field_acceptance_bundle.sh` 返回 `[PASS] verify_field_acceptance_bundle`，其中 `core='service=active sha=5ef7... index=assets/index-Bxrnerc2.js hls=#EXTM3U ptz=not-observed-in-recent-journal-window radar=...point cloud callback...'`，`video='ready onvif=True wssec=True ... file=014941-00000.mp4 stream=206 download=200'`，`scale='blocked: ui/scale_devices empty'`，`blind='blocked: processing diagnostics not ready'`。这说明当前仓库内软件链路已收口，剩余阻塞均为现场输入未就绪。
- 2026-04-19 01:59 的浏览器矩阵准备入口已验证通过：`rtk bash scripts/arm/verify_browser_matrix_readiness.sh` 返回 `[PASS] verify_browser_matrix_readiness`，并固定了真实浏览器应使用的地址 `monitor=http://100.105.175.44/monitor`、`HLS=/media/hls/camera/index.m3u8`、`WebRTC=/media/webrtc/camera/`；同时确认当前运行态编码基线为 `main=2k/20/auto`、`sub=720p/15/h264`、`record=h264`。这一步只证明“浏览器实测前置条件已就绪”，不替代真实有显示输出终端上的画面结论。
- 2026-04-19 02:00 的最新总验收复跑继续通过：`rtk bash scripts/arm/verify_field_acceptance_bundle.sh` 返回 `[PASS] verify_field_acceptance_bundle`，`core` 与 `video` 继续保持 `ready`，而 `scale='blocked: ui/scale_devices empty'`、`blind='blocked: processing diagnostics not ready'` 两项阻塞没有变化。
- 2026-04-19 02:00 之后新增了 `scripts/arm/generate_field_acceptance_report.sh`，它会复用 `verify_browser_matrix_readiness.sh` 和 `verify_field_acceptance_bundle.sh`，在 `logs/arm/` 下生成当前 ARM 现场验收的 markdown 证据报告，统一写入自动化结论、日志路径和剩余现场动作，避免现场再手工拼装多段回执。
- 2026-04-19 02:10 已实际生成一份新的现场证据报告：`logs/arm/20260419-020954-field-acceptance-report.md`。这份报告确认 `browser_status=pass`、`bundle_status=pass`，并把当前固定地址 `monitor=http://100.105.175.44/monitor`、`HLS=/media/hls/camera/index.m3u8`、`WebRTC=/media/webrtc/camera/` 以及仍待现场补齐的浏览器矩阵 / 称重协议 / 盲区补偿动作全部写成单一 markdown 文件。
- 2026-04-19 02:10 之后，`scripts/arm/generate_field_acceptance_report.sh` 还会同步刷新稳定路径 `logs/arm/latest-field-acceptance-report.md`。后续现场若只需要“最近一次自动化结论”，优先直接打开这个固定文件，不必再手工寻找最新时间戳报告。
- 2026-04-19 02:18 之后新增了 `scripts/arm/generate_remaining_acceptance_workpack.sh`，它会复用 `verify_browser_matrix_readiness.sh`、`verify_scale_protocol.sh`、`verify_blind_zone_workflow.sh`，在 `logs/arm/` 下生成一个时间戳工作包目录，并把浏览器矩阵、称重协议、盲区补偿三份正式模板前面自动补上一段当前运行态摘要，减少现场重复抄写 URL、阻塞原因和日志路径。
- `scripts/arm/generate_remaining_acceptance_workpack.sh` 同时会刷新稳定目录 `logs/arm/latest-remaining-acceptance-workpack/`。后续现场若只需要“最近一版可直接填写的草稿”，优先直接打开这个目录下的 `README.md`、`browser-matrix.md`、`scale-protocol.md`、`blind-zone-workflow.md`。
- 2026-04-19 02:25-02:33 已实际生成两轮剩余现场工作包；最新一份为 `logs/arm/20260419-023330-remaining-acceptance-workpack/`。这份工作包确认浏览器准备仍为 `PASS`，并把称重 `ui/scale_devices empty`、盲区 `processing diagnostics not ready` 两个当前阻塞直接预填到草稿页头，同时在 `README.md` 中追加了最后一步：回写完成后执行 `rtk bash scripts/arm/verify_remaining_acceptance_closure.sh`。
- 2026-04-19 02:35 之后新增了 `scripts/arm/ensure_current_acceptance_packet.sh`，它会把最新工作包里的浏览器/称重/盲区三份草稿，以及 `DOC/现场联调验收记录模板.md`，收敛成稳定目录 `DOC/当前现场验收包/`。默认只补种缺失文件，不覆盖现场已填写内容；需要重新初始化时可显式使用 `--refresh`。
- 2026-04-19 02:30 之后新增了 `scripts/arm/verify_remaining_acceptance_closure.sh`，它不再看运行态接口，而是优先确保 `DOC/当前现场验收包/` 已存在，再校验 `DOC/当前现场验收包/视频实机验收矩阵.md`、`DOC/当前现场验收包/现场联调验收记录.md`、`DOC/当前现场验收包/称重设备协议验收记录.md`、`DOC/当前现场验收包/盲区补偿参数试验记录.md`、`DOC/视频浏览器兼容矩阵.md`、`DOC/V1.1视频链路版本化验收清单.md` 和 `todo.md` 是否已经回写到可正式关单的状态；当前版本还会校验 `是否通过/是否已同步回写` 是否为肯定态，以及浏览器矩阵、当前验收包、现场联调记录三处关键允许清单是否一致。
- 2026-04-19 新增 `scripts/arm/finalize_remaining_acceptance_closure.sh`，它会先重跑 `verify_remaining_acceptance_closure.sh`；只有 gate 通过后才把 `DOC/当前现场验收包/`、`logs/arm/latest-field-acceptance-report.md`、`logs/arm/latest-remaining-acceptance-workpack/` 和 closure gate 日志一起归档到 `DOC/现场验收归档/<timestamp>-remaining-acceptance-closure/`，并同步更新 `DOC/V1.1视频链路版本化验收清单.md`、`todo.md`、`DOC/项目完成状态说明.md`，同时生成 `logs/arm/latest-remaining-acceptance-closure-report.md`。
- 2026-04-19 02:41 已再次确认：`rtk bash scripts/arm/generate_remaining_acceptance_workpack.sh` 新生成 `logs/arm/20260419-024108-remaining-acceptance-workpack/` 后，执行 `rtk bash scripts/arm/ensure_current_acceptance_packet.sh --refresh` 可把 `DOC/当前现场验收包/` 刷到同一轮数据。
- 2026-04-19 02:41 的最新 closure gate 实跑结果为 `[FAIL] verify_remaining_acceptance_closure`，当前机器判定的 blocker 已固定为：`video_matrix_has_placeholders`、`browser_allowlist_h265`、`browser_allowlist_force_h264`、`browser_allowlist_force_hls`、`field_record_*`、`scale_record_*`、`blind_record_*`、`v11_status_not_passed`、`v11_still_declares_not_passed`、`todo_still_has_remaining_items`。这条结果说明“最后一步还缺哪些回写”已经有稳定、可重跑且基于稳定现场包的自动化口径。
