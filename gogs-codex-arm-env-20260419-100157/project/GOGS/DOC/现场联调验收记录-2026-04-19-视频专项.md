# 现场联调验收记录 - 2026-04-19 视频专项

## 基本信息

- 日期：`2026-04-19`
- 现场：当前 ARM 测试机联调环境
- 目标机：`root@100.105.175.44`
- 编译机：`jamin@192.168.110.128`
- 后端版本：`sha256=8506b211713a4d72d7a9fdc0b827f2514f9d266aa70d05b7f7ad4e52f46ecc7a`
- 前端版本：首页资源 `assets/index-Bxrnerc2.js`
- 摄像头：`192.168.0.152`
- 雷达：`192.168.0.51`
- 记录人：Codex

## 一、ARM 目标机完整构建与联调

- 构建结果：通过编译机 `onebuild_GOGS_backend_self.sh` 构建最新工作树
- 部署结果：`rtk bash scripts/arm/deploy_backend.sh` 通过
- 启动结果：`gogs-backend.service=active`
- `verify_remote.sh`：`PASS`
- 备注：本轮后端同时修复了分段录像停止收口和视频文件回放路径归一化

## 二、ONVIF PTZ

- `status`：`ready=true`、`onvifConnected=true`、`wsSecurityEnabled=true`
- `move`：通过
- `relative-move`：通过
- `stop`：通过
- `home`：通过
- `presets`：可读到预置位列表
- `presets/goto`：通过
- 方向是否正确：接口链路已打通，最终方向手感仍建议在有显示输出的现场终端补一遍人工确认
- 速度是否正确：接口链路已打通，最终速度手感仍建议在有显示输出的现场终端补一遍人工确认
- 预置位是否兼容：当前真实设备可返回预置位并执行跳转

## 三、截图与录像

- 监控页顶部实时链路：`WebRTC 低延迟`
- 监控页顶部链路异常：`实时链路正常`
- 监控页顶部 MediaMTX：`正常`
- 监控页顶部端口探测：`API OK / HLS OK / WebRTC OK`
- 监控页顶部配置源：`rtsp://admin:dw159357@192.168.0.152:554/Streaming/Channels/101`
- 监控页顶部视频基线：`2K@20fps / 720p@15fps`
- 监控页顶部视频自检：`通过`
- 监控页顶部录像前提：`就绪`
- 监控页顶部录像健康：`healthy`
- 监控页顶部录像编码：`h264`
- `/api/video/self-check`：`overallStatus=ok`
- `snapshot`：返回 `image/jpeg`，当前样本 `2560x1440`，约 `244KB`
- `recording/start`：通过
- `recording/stop`：通过
- `recording/status`：停止后回到 `health=healthy`，`lastError=''`
- 录像文件：最新样本 `/userdata/GOGS/backend/data/videos/20260419/005943-00000.mp4`
- `ffprobe`：识别为可播放 `H.264 MP4`，时长约 `9.5s`
- `video-records`：可列出最新分段录像
- `video-files/stream`：返回 `206 Partial Content`
- `video-files/download`：返回 `200 OK`
- 导出文件可播放：是
- 长时录像稳定性：本轮仅完成短时实测，长时稳定性仍待独立现场项
- 备注：上一版后端存在 `moov atom not found` 和 `video file access denied`，本轮已在 ARM 目标机复验通过

## 四、浏览器侧补充说明

- 已用 headless Chromium 打开真实监控页，能看到监控页面壳、视频状态卡、自检摘要和点云/视频诊断信息。
- headless 浏览器本轮没有形成可信的实时画面证据，因此不能作为 `H.264/H.265` 兼容矩阵的最终结论。
- 最终浏览器矩阵仍需在有显示输出的真实 Chrome / Edge / Firefox / Safari 终端回写。

## 五、当前结论

- 已通过项：
  - ARM 目标机视频主链验活
  - ONVIF PTZ 接口链路
  - 后端抓图
  - 分段录像启动/停止/落盘
  - 回放列表、流式读取、文件下载
- 未通过项：
  - `H.264 / H.265` 真实浏览器兼容矩阵
  - `H.265` 允许清单
- 需要回写的 todo：
  - 浏览器兼容矩阵与允许清单
- 需要回写的 README：
  - `DOC/V1.1视频链路版本化验收清单.md`
  - `DOC/项目完成状态说明.md`
  - `todo.md`
