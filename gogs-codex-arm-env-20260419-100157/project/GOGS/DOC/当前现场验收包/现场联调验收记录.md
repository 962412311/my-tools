# 现场联调验收记录模板

先看 [`DOC/点云现场算法总览README.md`](/mnt/d/qtworkdata/gogs/DOC/点云现场算法总览README.md)，再填本模板中的算法相关结论。

当前现场实际填写优先落到 [`DOC/当前现场验收包/现场联调验收记录.md`](当前现场验收包/现场联调验收记录.md)。

## 基本信息

- 日期:
- 现场:
- 目标机:
- 后端版本:
- 前端版本:
- 相机型号:
- 雷达型号:
- PLC 型号:
- 记录人:

## 一、ARM 目标机完整构建与联调

- 构建结果:
- 部署结果:
- 启动结果:
- `verify-native-runtime.sh` 结果:
- 备注:

## 二、ONVIF PTZ

- `move`:
- `relative-move`:
- `stop`:
- `home`:
- `presets`:
- `presets/goto`:
- 方向是否正确:
- 速度是否正确:
- 预置位是否兼容:
- 备注:

## 三、截图与录像

- 监控页顶部实时链路:
- 监控页顶部链路异常:
- 监控页顶部 MediaMTX:
- 监控页顶部端口探测:
- 监控页顶部配置源:
- 监控页顶部视频基线:
- 监控页顶部视频自检:
- 监控页顶部录像前提:
- 监控页顶部录像健康:
- 监控页顶部插件数量:
- 监控页顶部缺失插件:
- 监控页顶部录像编码:
- `recoveryStatus / restartAttempts / lastRestartAt`:
- `retentionDays / lastPrunedCount / lastPrunedAt`:
- `/api/video/self-check` 的 `overallStatus / summaryMessage / checkItems`:
- `snapshot`:
- `recording/status`:
- `recording/start`:
- `recording/stop`:
- `GStreamer` 版本:
- 关键插件:
- 默认实时链路是否稳定维持在 `WebRTC`:
- 哪些场景会回退到 `HLS`:
- `recording/input_codec` 最终建议配置:
- 海康视频配置基线是否按“主码流录像 / 子码流预览 / 2K / 720p / 15fps~25fps”执行:
- `视频实机验收矩阵模板` 是否已同步回写:
- 允许直放 `H.265` 的浏览器/终端:
- 必须回退 `H.264` 的浏览器/终端:
- 导出文件可播放:
- 长时录像稳定性:
- 备注:

## 四、盲区补偿

- `annulusThicknessFactor`:
- `heightQuantile`:
- `volumeBlindZoneCoverageRatio`:
- `volumeBlindZoneDensityRatio`:
- `volumeBlindZoneSupportRatio`:
- `volumeBlindZoneCompensation`:
- 体积曲线是否平滑:
- 是否虚高:
- 是否长期空洞:
- 备注:

## 五、最高点与轨迹窗口

- `highestPointValid`:
- `highestPointCandidateCount`:
- `trajectoryFlushReason`:
- `trajectoryWindowFrames`:
- `trajectoryWindowDistance`:
- 最高点是否稳定:
- 轨迹切块是否合理:
- 静止/停机时尾段是否入图:
- 备注:

## 六、低置信度补扫

- `rescanSuggested`:
- `rescanSuggestedReason`:
- `blockedReason`:
- `suggestedTarget`:
- `controlPhase`:
- 是否只停留在建议态:
- 是否允许自动执行:
- 备注:

## 七、结论

- 是否通过:
- 未通过项:
- 需要回写的 todo:
- 需要回写的 README:

## 八、推荐附加记录

- 设备与环境版本:
- 成功链路:
- 失败链路:
- 稳定复现步骤:
- 当前临时绕过方式:
- 后续代码修复项:
