# 项目完成状态说明

截至当前仓库状态，项目的**代码主线、前端主线、数据库自举、PLC 通讯、点云处理、原生部署文档**均已收口完成，仓库内不再保留已知的未接入草稿模块或 mock 主线。

## 已完成范围

- 后端采用 Qt 原生底座：
  - 点云接入与处理
  - HTTP / WebSocket
  - QtSql 数据库接口
  - Qt SerialBus PLC 通讯
  - 原生配置、日志和运行目录
- 雷达 SDK 已统一接入为双平台方案：
  - Windows 编译 `backend/SDK/LidarView/arm linux 2026123/sdk` 中的 SDK 源码 wrapper，并在运行时携带 `backend/SDK/LidarView/win v2.0.6`
  - ARM Linux 使用 `backend/SDK/LidarView/arm linux 2026123/sdk`
  - 原 SDK 文件保持原封不动，通过编译宏区分平台；Windows 侧不再硬依赖 `win v2.0.6` 中不存在的开发库
  - 上层业务接口仍保持为统一的 `LidarDriver`
- Windows 下的 QtSql `QMYSQL` 插件运行说明已补充：
  - MSVC 链接 `C:/vcpkg/installed/x64-windows/lib/libmysql.lib`
  - 运行目录需要可见 `libmysql.dll`
- 前端已切换为真实后端接口链路。
- 前端本地自动化回归已补齐：
  - `node:test` 契约回归 `164` 项通过
  - `vite build` 生产构建通过
  - Playwright smoke 已覆盖 `Dashboard / Monitor / Playback / 关键 route shell`
- 文档已统一为：
  - `ARM Debian 11`
  - `Qt 6.2.4`
  - `PCL 1.11.1`
  - `Eigen3 3.3.9`
  - 独立前端 + 原生后端
- 数据库启动链已支持：
  - 自动建库
  - 自动建表
  - 自动补列
  - 自动补索引
- 配置文件加载时会回填缺失或空白项，确保数据库等关键配置在首次启动后完整落盘
- 旧配置文件如果缺少数据库段，启动时会自动重建并补齐
- 独立称重设备实时采集链路已经接入后端主动轮询桥，支持串口/网口 Modbus 设备的实时重量、采样时间和硬件在线状态回填；现场仍需按真实设备协议做最终核对
- 称重设备协议核对固定入口已补齐：
  - `scripts/arm/verify_scale_protocol.sh` 已固化 `/api/scales/status` 的设备清单、采样新鲜度和关键寄存器映射体检
  - `DOC/称重设备现场协议核对SOP.md` 与 `DOC/称重设备协议验收记录模板.md` 已补齐现场回写口径
- 点云融合新主链已经收口：
  - `FusionSegmentService::measurementCloudReady` 已正式接入盘存体积、料堆库存和历史留痕主链
  - `fusionPreview` 已从 `globalMap` 兼容透传中剥离，监控页区分融合预览与全局地图两条显示语义
- 盲区补偿慢速扫描验收入口已补齐：
  - `scripts/arm/verify_blind_zone_workflow.sh` 已固化 `config / rescan status / rescan analyze / support-coverage-density` 摘要
  - `DOC/点云盲区补偿慢速扫描验收标准.md` 与 `DOC/现场联调盲区补偿参数试验记录模板.md` 已补齐参数试验和通过标准
- ARM 发布与回归链路已再次打通：
  - `2026-04-18` 已通过可达编译地址 `jamin@192.168.110.128` 完成一次最新工作树的 ARM 构建、后端替换和前端同步
  - `2026-04-19` 最新目标机运行后端 `GrabSystem` `sha256=5ef7aba908e780d3cac5527dd4445f5cdb81b4a2b0ccf1942356ad918c836104`
  - 测试机首页最新资源已切到 `assets/index-Bxrnerc2.js`
  - `scripts/arm/run_backend_regression.sh` 已改为每轮独立临时路径 + `timeout` + 退出清理，避免旧回归进程占住固定 `/tmp/GrabSystemRegressionTests.arm`
  - `rtk bash scripts/arm/run_backend_regression.sh` 在最新脚本口径下继续返回 `All regression checks passed`
  - `2026-04-19` 运行配置已确认切到 `192.168.0.51` 雷达 + `192.168.0.152` 摄像头，旧的 `192.168.0.52 / 192.168.0.133` 组合当前关闭
  - 在 `51/152` 组合下，`rtk bash scripts/arm/verify_remote.sh` 已返回 `PASS`；当前 `core` 验收稳定要求为服务状态、运行态 sha、首页资源、`#EXTM3U` 和最近雷达回调，近期未发生 PTZ 动作时只记录 `ptz=not-observed-in-recent-journal-window`，避免总验收入口被最近日志窗口误杀
  - `rtk bash scripts/arm/verify_field_acceptance_bundle.sh` 已在当前运行态返回 `PASS`，汇总为 `core=ready`、`video=ready`、`scale=blocked: ui/scale_devices empty`、`blind=blocked: processing diagnostics not ready`
  - `rtk bash scripts/arm/generate_field_acceptance_report.sh` 已可一键导出当前 ARM 现场验收的 markdown 证据报告，把浏览器矩阵准备结论、现场总验收摘要、日志路径和剩余现场动作收口到单一文件
  - `rtk bash scripts/arm/generate_remaining_acceptance_workpack.sh` 已可一键导出三份剩余现场项的预填草稿，把浏览器矩阵、称重协议和盲区补偿的当前自动化结果直接落到可填写工作包中
  - `rtk bash scripts/arm/ensure_current_acceptance_packet.sh` 已补齐为稳定回写包入口，把最终现场实际填写目标固定到 `DOC/当前现场验收包/`
  - `rtk bash scripts/arm/verify_remaining_acceptance_closure.sh` 已补齐为最终收口门槛脚本，用来判断浏览器矩阵、现场联调记录、称重记录、盲区记录、`V1.1` 清单和 `todo` 是否已全部回写到可正式关单的状态；当前 gate 还会校验 `是否通过/是否已同步回写` 的肯定态，以及浏览器矩阵、当前验收包、现场联调记录三处关键允许清单是否一致
  - `rtk bash scripts/arm/finalize_remaining_acceptance_closure.sh` 已补齐为正式关单执行入口；在 closure gate 通过后，它会把 `DOC/当前现场验收包/`、`logs/arm/latest-field-acceptance-report.md`、`logs/arm/latest-remaining-acceptance-workpack/` 和 closure gate 日志一并归档到 `DOC/现场验收归档/`，同步更新 `V1.1`、`todo.md` 和 `DOC/项目完成状态说明.md`，并导出稳定的最终收口报告
  - 最新一份自动生成报告已落到 `logs/arm/20260419-020954-field-acceptance-report.md`
  - 同时会刷新稳定路径 `logs/arm/latest-field-acceptance-report.md`，便于现场直接引用最近一次报告
  - 最新一份剩余现场工作包已落到 `logs/arm/20260419-024108-remaining-acceptance-workpack/`
  - 同时会刷新稳定目录 `logs/arm/latest-remaining-acceptance-workpack/`，便于现场直接填写最近一版草稿
  - 同时会补种稳定目录 `DOC/当前现场验收包/`，默认不覆盖现场已填写内容；如需按最新工作包重置，可执行 `rtk bash scripts/arm/ensure_current_acceptance_packet.sh --refresh`
  - `2026-04-19 02:41` 已确认 `DOC/当前现场验收包/` 与 `logs/arm/latest-remaining-acceptance-workpack/` 保持同轮数据
  - `2026-04-19` 当前 closure gate 实跑结论仍是 `FAIL`，明确阻塞为：浏览器实机矩阵与允许清单未回写、现场联调验收记录未填、称重协议记录未填、盲区补偿记录未填、`V1.1` 仍是 `验收中`、`todo.md` 仍有剩余待办
- 摄像头相关软件主线已经收口：
  - 标准 `ONVIF PTZ`
  - `WS-Security UsernameToken Digest`
  - `SnapshotUri` 后端兜底抓图
  - `RTSP` 实时预览
  - `GStreamer` 后端录像兜底
  - `recording/input_codec` 的 `自动` / `H.264` / `H.265` 切换
  - `camera/main_stream_*` / `camera/sub_stream_*` 结构化视频基线字段
  - 配置页海康视频配置基线摘要，固定“主码流录像 / 子码流预览 / 2K 主码流 / 720p 子码流 / 15fps~25fps”的现场建议口径
  - `/api/video/recording/status` 的 `healthStatus / healthMessage / checkedAt` 录像健康诊断字段
  - `/api/video/recording/status` 的 `recoveryStatus / restartAttempts / lastRestartAt` 自动恢复留痕字段
  - `/api/video/recording/status` 的 `retentionDays / lastPrunedCount / lastPrunedAt` 保留策略留痕字段
  - `/api/video/self-check` 的 `overallStatus / summaryMessage / checkItems` 统一视频自检摘要
  - 监控页顶部实时链路、链路异常、`MediaMTX` 健康、端口探测、配置源、视频基线、录像前提、录像健康、插件数量和录像编码的可见状态
  - 现场联调时按“实时链路 -> 链路异常 -> MediaMTX -> 端口探测/配置源 -> 录像前提 -> 插件 -> 录像编码”的固定判断顺序排障
  - 海康 `RTSP / ONVIF / ISAPI / SDK` 能力边界文档已补齐
  - 海康设备联调 `SOP` 已补齐，接入、排障和回写顺序已固定
  - `V1.1` 视频链路版本化验收清单已补齐，当前已具备正式验收入口
  - `scripts/arm/verify_browser_matrix_readiness.sh` 已固化真实浏览器矩阵验收前的准备入口，可自动输出当前 `monitor / HLS / WebRTC player` 地址、主/子码流编码基线和录像输入编码
  - 当前实机准备摘要已固定为 `monitor=http://100.105.175.44/monitor`、`HLS=/media/hls/camera/index.m3u8`、`WebRTC=/media/webrtc/camera/`，运行态编码基线为 `main=2k/20/auto`、`sub=720p/15/h264`、`record=h264`
  - `2026-04-19` 已在真实设备对 `51/152` 上完成 `PTZ / 抓图 / 分段录像 / 回放接口` 专项复验：
    - `PTZ status / stop / move / relative-move / presets / goto preset / home` 通过
    - `/api/video/snapshot` 返回 `image/jpeg`
    - 分段录像样本 `005943-00000.mp4` 已通过 `ffprobe`
    - `/api/video-files/stream` 返回 `206 Partial Content`
    - `/api/video-files/download` 返回 `200 OK`
    - `scripts/arm/verify_video_workflow.sh` 已固化为可重跑的视频专项验收入口
    - 最新重跑持续通过，当前运行态会生成新的分段录像样本并继续通过 `ffprobe`，`stream=206`、`download=200`

## 当前仍需现场验收项

- 真实摄像头环境回写 `H.264 / H.265` 浏览器兼容矩阵与 `H.265` 允许清单。
- 在有显示输出的真实浏览器/终端上完成 `WebRTC / HLS` 最终画面验收。
- 真实称重设备上按固定 SOP 回写寄存器映射、心跳周期、重量换算系数和异常码语义。
- 真实料堆慢速扫描场景按固定标准回写盲区补偿参数结论。
- `2026-04-19` 当前测试机专项脚本实跑结果也已明确：
  - `verify_field_acceptance_bundle.sh` 已汇总为“软件链路 ready，现场专项部分 blocked”
  - `verify_scale_protocol.sh` 返回 `ui/scale_devices is empty`，说明称重设备尚未在测试机当前运行态落配置
  - `verify_blind_zone_workflow.sh` 返回 `processing diagnostics are not ready`，说明当前还没有可用于盲区参数验收的活跃处理诊断

这些项目都属于**外部运行环境验证或设备协议最终落地**。从仓库内部实现角度看，摄像头主线已经完成收口，`PTZ / 抓图 / 录像 / 回放接口` 也已在真实设备对 `51/152` 上形成通过证据；剩余主要是浏览器矩阵、称重协议和盲区补偿这三类现场最终验收项。

## 结论

从仓库内部实现角度看，项目主线已经完成收口，摄像头软件链路已经进入“接口与目标机链路已通过、浏览器矩阵待补齐”的状态；
从最终交付验收角度看，还需要以上现场项完成验证作为最后一步。
