# Claude API Skill Archive

这份归档用于保存本地 `claude-api` skill 的可追踪版本，方便检查 frontmatter、触发描述和正文是否一致。

## 内容

- [`SKILL.md`](SKILL.md)：修正为合法 YAML frontmatter 的 skill 正文

## 说明

- `description` 必须保持为合法 YAML 字符串
- 描述字段应只表达触发条件，不应复述完整工作流
- 需要同步官方本地 skill 修订时，优先更新这里的归档版本
